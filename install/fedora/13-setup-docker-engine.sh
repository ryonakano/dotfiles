#!/bin/bash

################################################################################
# Setup prerequisites for rootless mode
# See https://docs.docker.com/engine/security/rootless/#prerequisites
################################################################################
# Fedora meets prerequisites by default, so NOP
