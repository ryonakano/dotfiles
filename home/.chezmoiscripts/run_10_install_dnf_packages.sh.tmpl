#!/bin/bash

which dnf > /dev/null
if [ $? -ne 0 ]; then
	exit 0
fi

# Common packages
PKGS_DNF="
	ibus-skk
"

if [[ "$HOSTNAME" =~ ^.*-host$ ]]; then
	# Host tools
	PKGS_DNF="$PKGS_DNF
		flatpak
	"
fi

if [ "$PKGS_DNF" ]; then
	sudo dnf install -y $PKGS_DNF
fi
