#!/bin/bash

which apt > /dev/null
if [ $? -ne 0 ]; then
	exit 0
fi

# Common packages
PKGS_APT="
	ibus-skk
"

if [[ "$HOSTNAME" =~ ^.*-host$ ]]; then
	# Host tools
	PKGS_APT="$PKGS_APT
		flatpak
	"
fi

if [ "$PKGS_APT" ]; then
	sudo apt install -y $PKGS_APT
fi
