#!/bin/bash
# dconf.ini hash: {{ joinPath .chezmoi.sourceDir "config" .chezmoi.osRelease.id "dconf.ini" | include | sha256sum }}

{{ if eq .chezmoi.osRelease.id "fedora" }}

CONFIG_DIR={{ joinPath .chezmoi.sourceDir "config" .chezmoi.osRelease.id | quote }}

dconf load / < $CONFIG_DIR/dconf.ini

{{ end }}
